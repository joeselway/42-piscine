#include <unistd.h>
#include <stdio.h>

int	main(int argc, char** argv)
{
	int i;
	i = 0;
	if (argc == 2)
	{
		while argv[1][i] != '\0'
		{
			if (av[1][i]
	}

/*
	while(*argv)
	{
		printf("the printf output is: %s\n", *argv);
		write(1, *argv[0], strlen(*argv);
		argv++;
	}
 
*/
